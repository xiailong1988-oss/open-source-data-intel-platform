# 当前任务说明

## 当前状态

- 截至 `2026-04-09`，`doc/tasks/当前任务/` 已完成一次按状态清理。
- 已明确完成的任务文档与设计稿，已统一转入 `doc/tasks/已执行任务/`。
- 尚未收口的 `任务-首页与全局字体排版收口.md` 已转入 `doc/tasks/待执行任务/`。
- 当前目录暂不保留正在执行中的任务文档。

## 使用约定

1. 新任务开始前，先在 `doc/tasks/当前任务/` 建立任务文档。
2. 任务完成并形成结果记录后，再转入 `doc/tasks/已执行任务/`。
3. 暂不执行、尚未完成或等待重新排期的任务，放入 `doc/tasks/待执行任务/`。
4. 如果只是历史设计稿或中间过程稿，但对应任务已经完成，也一并归入 `doc/tasks/已执行任务/`，避免继续占用“当前任务”语义。
