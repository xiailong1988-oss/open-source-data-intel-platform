# 当前执行状态

## 当前执行位置
当前任务：
- `doc/tasks/当前任务/任务-首页布局与组件机制重构.md`

当前状态：
- 已完成“综合态势首页布局与组件机制重构”
- 当前处于“已完成，待人工验收”状态

## 本轮任务结论
本轮不再继续围绕第三阶段首页做轻量美化，而是直接修正了首页的布局机制问题：
1. 地图不再以过高的固定姿态吞掉首屏高度，而是改为按比例自适应缩小；
2. 左侧系统统计区改为纯数字快照，不再堆积冗长说明；
3. 底部六大板块回到原始需求体系；
4. 左侧和底部从“只能排序”的半成品，升级为基于 GridStack 的拖拽 + resize + 布局持久化；
5. 地图继续固定为中间主舞台，不参与拖拽与 resize；
6. 右侧 signal rail 继续保持“自动滚动不驱动地图、点击条目才联动地图并打开详情”的正确方向。

## 本轮已完成事项
### 首页布局机制
- 首页继续保持“左中右下”四区结构：
  1. 顶部极轻状态条
  2. 左侧系统统计区
  3. 中央视态地图舞台
  4. 右侧重要情报流
  5. 底部六大板块摘要矩阵
- 已重写首页主体区高度策略：
  - 地图主舞台缩小一档
  - 主体区高度按比例自适应
  - 首屏完整露出底部六大板块摘要矩阵
- 已确认桌面端 `1366x768`、`1600x900` 下首页不再出现“地图过高挤没底部”的问题。

### 左侧系统统计区
- 已将左侧系统统计区改为“纯数字总览”：
  - 仅保留标题、数字、单位、状态点
  - 去掉冗长解释性描述
- 已保留系统统计区的 4 个系统模块：
  - 采集能力
  - 数据存储
  - 系统资源
  - 智能体与模型
- 已接入：
  - `GridStack`
  - 拖拽换位
  - 高度 resize
  - localStorage 布局持久化
  - 恢复默认布局
  - count-up 数字动效

### 中央视态地图舞台
- 已保留：
  - 海淀边界
  - 点位图层
  - 焦点高亮
  - 地图点击联动
  - 顶部极窄 strip
- 已修正地图高度机制：
  - 不再使用过高的最小高度策略
  - 改为跟随首页主体区比例收缩
  - 不再压缩底部矩阵
- 已继续保留“一键显示全部重要信息”能力：
  - 顶端/底端吸附
  - SVG 联线指向真实点位
  - hover 同步高亮
  - 点击联线项进入详情
- 地图仍然固定在中间主舞台，不参与首页布局拖拽和 resize。

### 右侧 signal rail
- 已保留正确交互，不回退：
  - 自动滚动只推动 rail 本身
  - 自动滚动不联动地图
  - hover 暂停 / 移出恢复
  - 点击 ticker 时地图聚焦并打开详情
- 右侧 rail 继续固定在地图右侧，不参与拖拽布局。

### 底部六大板块摘要矩阵
- 已将底部六大板块回到原始需求体系，当前为：
  1. 产业发展
  2. 科技创新
  3. 特色经济
  4. 民生服务
  5. 安全治理
  6. 城乡建设
- 每个格子已收敛为：首页摘要卡，而不是详情面板：
  - 板块标题
  - 2～4 个关键指标
  - 一个状态/趋势标识
- 已接入：
  - `GridStack`
  - 拖拽换位
  - 宽高 resize
  - 布局持久化
  - 恢复默认布局
  - 点击跳转对应详情页

### 技术接入
- 已新增并接入：
  - `gridstack`
  - `gridstack/dist/gridstack.min.css`
- 已继续保留：
  - `@vueuse/motion`
- 首页布局引擎当前采用：
  - 左侧模块一列 GridStack
  - 底部六格独立 GridStack
  - 地图和右侧 rail 固定

### 文档同步
- 已新增并回写当前任务文档：
  - `doc/tasks/当前任务/任务-首页布局与组件机制重构.md`
- 已回写当前执行状态：
  - `doc/rules/当前执行状态.md`

## 验证结果
### 构建
- `npm run build`：通过

### 定向首页布局回归
已完成桌面端定向回归：
- `1366x768`
- `1600x900`

已验证：
1. 首页保持“左中右下”四区结构；
2. 右侧 rail 稳定在地图右边；
3. 首屏完整露出底部六大板块摘要矩阵；
4. 底部六大板块命名已切回原始需求体系；
5. 左侧 GridStack resize 后会持久化；
6. 底部 GridStack resize 后会持久化；
7. 自动滚动仍然不会带动地图焦点变化。

### 定向首页交互回归
已完成首页链路验证：
1. 左侧系统统计点击可跳转；
2. 底部六大板块点击可跳转；
3. 右侧 rail 点击可联动地图并打开详情；
4. 地图联线项点击可打开详情页；
5. 返回首页后首页主舞台可继续正常渲染。

## 当前存在问题
当前没有已知阻断性问题。
仍可作为后续深化项的方向：
1. 将左侧与底部 GridStack 的持久化逻辑进一步抽成统一 composable；
2. 为 GridStack resize 增加更直观的视觉提示；
3. 补一套专门针对首页布局机制的轻量 Playwright 脚本，替代当前较重的全量 `ui-smoke`；
4. 继续优化地图联线层的避让算法与批量显示策略。

## 下一步建议
先进行人工验收，重点看：
1. 首屏是否完整看到底部六大板块；
2. 地图高度是否终于合理；
3. 左侧系统统计是否已经变成纯数字快照；
4. 底部六大板块命名是否符合原始需求；
5. 左侧和底部的拖拽、resize、布局保存是否符合预期；
6. 地图“一键显示全部重要信息”联线层是否仍然正常。
